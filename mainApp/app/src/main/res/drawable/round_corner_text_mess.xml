<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android = "http://schemas.android.com/apk/res/android"
    android:shape = "rectangle">
    <corners android:radius="15dp" />

    <gradient
        android:angle="90"
        android:centerColor="@color/colorMess"
        android:startColor="@color/colorMess"
        android:endColor="@color/colorMess"/>

    <stroke
        android:width="1dp"
        android:color="@color/colorMess"/>

    <padding
        android:bottom="15dp"
        android:left="15dp"
        android:right="15dp"
        android:top="15dp" />
</shape>